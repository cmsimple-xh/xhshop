If you don't want to mess around with the original xhshop-templates,
here is a chance to overwrite them.

Xhshop will first look into this subfolders, if there is a customized
.tpl - if not, it will load the "orginal"